let variable: string | number;
variable = "Hello";
console.log(variable);

variable = 32;
console.log(variable);

let state: "enable" | "disable";

state = "enable";
console.log(state);

state = "disable";
console.log(state);
